export type AnalyticsKpiCard = {
  title: string;
  value: string;
  change: string;
};

export type TopPlacement = {
  id: number;
  slot: string;
  impressions: string;
  clicks: string;
  ctr: string;
  revenue: string;
};
